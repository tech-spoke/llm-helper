# MCP Server
mcp>=1.0.0

# v3.9: ChromaDB for semantic search
chromadb>=1.0.0

# Tree-sitter for code structure analysis
# Note: tree-sitter 0.22+ breaks compatibility with tree-sitter-languages
tree-sitter>=0.21.0,<0.22.0
tree-sitter-languages>=1.10.0

# For async subprocess handling
asyncio

# v3.7: Embedding for semantic similarity
sentence-transformers>=2.2.0
scikit-learn>=1.0.0

# v1.1: YAML for context configuration
PyYAML>=6.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
